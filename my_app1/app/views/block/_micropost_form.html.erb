<%= form_for(@micropost) do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new micropost...", style: "width: 300px; height:80px;" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>