
Trang quản lý người dùng thì vào đây nhé
	http://cosplay.framgia.vn/sign_up
	account: do mình tạo trên ios hoặc android
Trang quản lý admin: http://cosplay.framgia.vn/admin/sign_up 
	user: admin@cosplay-no1.com pass: 12345678

Supportership.where(supporter_id: @user.id).group(:supportee_id)

- @posts.each do |post|
  = link_to request.base_url+admin_post_path(post), admin_post_path(post)

Lúc đầu tôi muốn tất cả những tham số khi gọi đến gửi mail là bạn phải truyền vào. Nhưng sau đó bạn đã comment về việc này, nên tôi đã viết lại để tự lấy những tham số đó. 

Mailer
@supporties = Supportership.where(supporter_id: @user.id).group(:supportee_id) @posts = Supportership.where(supporter_id: @user.id).group(:post_id)


I've created your account at staging server.
Could you check whether to be able to access?

IP address is 10.0.1.34.
Acount: khoipk
Password: password

Run: 
mysql -uroot -ppassword -S /run/mysql-cosplay_server/mysqld.sock

git rebase -i HEAD~2

.where(device_uid: "357656053413306", registering_way: "email").includes(:request_token).order("request_tokens.updated_at DESC").firstgit 

bundle exec rspec spec/models/lottery_spec.rb

RAILS_ENV=test bundle exec rake db:drop db:create db:migrate db:seed

APNS_CERTIFICATE_PATH=~/Downloads/ck.pem APNS_PASSWORD=123456 GCM_AUTH_KEY=AIzaSyChkUj2wYFpEnutkIWd218G-6HKQgYRKq0 bundle exec rake db:drop db:create db:migrate db:seed

rails generate migration add_lock_version_to_posts lock_version:integer 

http://dev.framgia.com/redmine/issues/32009
http://dev.framgia.com/redmine/issues/34696
http://dev.framgia.com/redmine/issues/30912
Có mấy cái này a cần server support mới code đc. Em check giúp anh nhé

Delete: Cron tab ps aux | grep delayed
kill -9 27277
RAILS_ENV=staging bin/delayed_job restart

/usr/local/rails_apps/cosplay_server/current
